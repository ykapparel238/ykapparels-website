# Theme first-time visitors see: light | dark | system
# (system = follow the visitor's OS preference)
# Visitors' own toggle choice is stored in their browser and always wins.
PUBLIC_DEFAULT_THEME=light
# Optional: set this in Cloudflare Pages/Workers to enable consent-gated GA4.
PUBLIC_GA_MEASUREMENT_ID=
